// Given a string of alphabetical characters, how would you compress it? For example, ‘aaabbbcc’ => ‘a3b3c2’. 
// The output string’s length is always equal to or smaller than the input string

const sum = (a,b) => {
    return a + b
}

module.exports = sum